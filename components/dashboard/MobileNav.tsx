"use client";

// Mobile nav temporarily removed per user request.
// Keep file and exports to respect repo guardrails; render nothing.

export function MobileNav({ className }: { className?: string }) {
  return null;
}
